import 'package:flutter/material.dart';

class ChatRoomViewBody extends StatelessWidget {
  const ChatRoomViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
